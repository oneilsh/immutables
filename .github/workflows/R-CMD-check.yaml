name: R-CMD-check

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

concurrency:
  group: r-cmd-check-${{ github.ref }}
  cancel-in-progress: true

jobs:
  R-CMD-check:
    runs-on: ubuntu-latest
    container: rocker/r2u:latest
    steps:
      - uses: actions/checkout@v4

      - name: Install system dependencies
        run: |
          apt-get update
          apt-get install -y --no-install-recommends pandoc

      - name: Install R dependencies
        run: |
          Rscript -e 'install.packages(c("Rcpp","lambda.r","knitr","rmarkdown","testthat","rcmdcheck"), repos = "https://cloud.r-project.org")'

      - name: Check
        env:
          _R_CHECK_FORCE_SUGGESTS_: "false"
        run: rcmdcheck::rcmdcheck(build_args = c("--no-build-vignettes"), args = c("--no-manual", "--ignore-vignettes"), error_on = "warning")
        shell: Rscript {0}
