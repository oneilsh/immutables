% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/30-api-apply-generics.R,
%   R/30-api-flexseq-seq-apply.R, R/40-ordered_multiset-core.R,
%   R/40-priority_queue-core.R
\name{apply}
\alias{apply}
\alias{apply.flexseq}
\alias{apply.ordered_multiset}
\alias{apply.priority_queue}
\title{Apply with S3 dispatch}
\usage{
apply(X, MARGIN, FUN, ...)

\method{apply}{flexseq}(X, MARGIN = NULL, FUN = NULL, ..., preserve_monoids = FALSE)

\method{apply}{ordered_multiset}(X, MARGIN = NULL, FUN = NULL, ..., reset_ties = FALSE)

\method{apply}{priority_queue}(X, MARGIN = NULL, FUN = NULL, ..., reset_ties = TRUE)
}
\arguments{
\item{X}{Object to apply over.}

\item{MARGIN}{For base arrays, margin(s) to operate over.}

\item{FUN}{Function to apply.}

\item{...}{Method-specific arguments.}

\item{preserve_monoids}{Logical; when `TRUE`, carries all input monoids to
the output. When `FALSE` (default), output keeps only invariant monoids.}

\item{reset_ties}{Logical; if `TRUE`, refreshes tie-break `seq_id` by
current object order. If `FALSE`, preserves existing `seq_id` values.}
}
\value{
Method-dependent result.
}
\description{
`apply()` is exported as an S3 generic so immutable structures can provide
specialized behavior while preserving base array behavior by default.
}
\details{
For immutable structures, use `apply(x, FUN, ...)` (no `MARGIN`):
- `flexseq`: `FUN(value, ...)` returns transformed value.
- `priority_queue`: `FUN(item, priority, seq_id, name, ...)` returns a named
  list using fields from `item`, `priority`, `name`.
- `ordered_multiset`: `FUN(item, key, seq_id, name, ...)` returns a named
  list using fields from `item`, `key`, `name`.
}
