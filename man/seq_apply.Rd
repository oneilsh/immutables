% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/30-api-flexseq-seq-apply.R
\name{seq_apply}
\alias{seq_apply}
\title{Apply a Function to Each Sequence Element}
\usage{
seq_apply(x, f, preserve_monoids = FALSE, ...)
}
\arguments{
\item{x}{A `flexseq`.}

\item{f}{Function applied to each element.}

\item{preserve_monoids}{Logical; when `TRUE`, carries all input monoids to
the output. When `FALSE` (default), output keeps only invariant monoids
(`.size`, `.named_count`).}

\item{...}{Additional arguments passed to `f`.}
}
\value{
A `flexseq` with transformed elements.
}
\description{
Maps `f` over elements of a `flexseq` and returns a new `flexseq`.
Traversal is linear, and rebuild uses standard tree construction.
}
\examples{
x <- as_flexseq(1:5)
seq_apply(x, function(v) v * 10)

xn <- as_flexseq(setNames(as.list(1:3), c("a", "b", "c")))
seq_apply(xn, function(v) v + 1)
}
