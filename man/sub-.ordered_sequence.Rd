% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/30-api-flexseq-indexing.R
\name{[.ordered_sequence}
\alias{[.ordered_sequence}
\alias{[[.ordered_sequence}
\alias{[<-.ordered_sequence}
\alias{[[<-.ordered_sequence}
\title{Index an ordered sequence}
\usage{
\method{[}{ordered_sequence}(x, i, ...)

\method{[[}{ordered_sequence}(x, i, ...)

\method{[}{ordered_sequence}(x, i) <- value

\method{[[}{ordered_sequence}(x, i) <- value
}
\arguments{
\item{x}{An `ordered_sequence`.}

\item{i}{For `[`, positive integer indices, character names, or logical mask.}

\item{...}{Unused.}

\item{value}{Replacement value (unsupported).}
}
\value{
For `[`, an `ordered_sequence` subset that preserves key order.

For `[[`, one payload element by scalar integer position or scalar
  character name.

No return value; always errors because replacement indexing is unsupported.

No return value; always errors because replacement indexing is unsupported.
}
\description{
Ordered sequences support read indexing while preserving key-order semantics.
}
\details{
For `[`:
- integer and logical indices must resolve to strictly increasing positions;
- character indices are resolved by names and must also be strictly
  increasing;
- duplicates and reordering are rejected.

For `[[`:
- accepts scalar integer position or scalar character name and returns the
  payload element.

Replacement indexing (`[<-`, `[[<-`) is intentionally unsupported.
}
\examples{
x <- as_ordered_sequence(list("b", "a", "c"), keys = c(2, 1, 3))
x[1:2]
try(x[c(2, 1)])
}
\seealso{
[\[.flexseq], [\[<-.ordered_sequence], [\[\[<-.ordered_sequence]
}
