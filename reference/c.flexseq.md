# Concatenate Sequences

Concatenate Sequences

## Usage

``` r
# S3 method for class 'flexseq'
c(..., recursive = FALSE)
```

## Arguments

- ...:

  \`flexseq\` objects.

- recursive:

  Unused; must be \`FALSE\`.

## Value

A concatenated \`flexseq\`.
