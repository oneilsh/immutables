# Get interval bounds in sequence order

Get interval bounds in sequence order

## Usage

``` r
interval_bounds(x)
```

## Arguments

- x:

  An \`interval_index\`.

## Value

Data frame with list-columns \`start\` and \`end\`.
